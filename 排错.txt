找错三要素:消息类型行号

1、不能再res.send之后再有任何代码段
注意:所有的整个服务器代码只能执行一条res.send()
解决:保证只有一条res.send语句执行即可

2、router.use函数参数需要一个中间件函数但是得到了一个对象
解决:
1)导出路由错了
2)引入路由错误
3)挂载错误

3、访问3000端口被拒绝
解决方案:
1)关闭之前占用的3000端口
2)换一个新的端口号

4、在前端执行JSON.parse时出错，解析的数据格式有问题
解决:检查服务器端返回的数据格式

5、语法错误
解决:检查语法格式

6、链接127.0.0.1:3306 端口 被拒绝
解决:打开mysql服务器

7、不能读取一个值为undefined的length属性
解决:
result为空，sql语句sql注入，
就近原则，从近的开始找，寻找数据最初来源

8、跨域错误
解决:
1)不要ctrl+b运行  不要双击运行
2)要在自己的nodejs服务器端口下运行

9、xhr对象未定义
解决:
1)创建小黄人对象出错  
2)注意函数的作用域

10、404路径错误
1)后端接口错误
2)前端ajax请求端口错误
3)前后端get和post请求方式不一致

f12  后点击network











